<h4>Steps</h4>

<div class="row">
  <div class="col s12">
    {{#each sortedSteps as |step|}}
      {{step-card step=step}}
    {{/each}}    
  </div>
</div>

<div class="row">
  {{#link-to 'pipelines.index' class='btn'}}Back{{/link-to}}
</div>

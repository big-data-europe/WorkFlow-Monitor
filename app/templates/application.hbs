<div class="navbar-fixed">
  {{#md-navbar name='BDE Workflow Monitor' homeRoute='application'}}
    {{#link-to 'pipelines.index' tagName='li'}}
      {{link-to 'Workflows' 'pipelines.index'}}
    {{/link-to}}
  {{/md-navbar}}
</div>

<div class="container">
  <div class="row">
    <div class="col s12">
      {{outlet}}
    </div>
  </div>
</div>

{{!-- ember-cli-materialize modal container --}}
{{md-modal-container}}

